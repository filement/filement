#!/bin/bash

if [ -f ../miniupnpc.a ]; then exit; fi

export CC=gcc
export CFLAGS=
export LDFLAGS=

src='miniupnpc_filement'

tar xfj "${src}.tar.bz2" && \
cd "$src" && \
make clean > /dev/null && \
make > /dev/null
cp libminiupnpc_filement.a ../../libminiupnpc.a && \
cd .. && \
rm -r "$src"
