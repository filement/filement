#!/bin/bash

if [ -f ../miniupnpc.a ]; then exit; fi

# TODO this is only for linux
export CC=gcc

src='miniupnpc_filement'

tar xfj "${src}.tar.bz2" && \
cd "$src" && \
make clean > /dev/null && \
make > /dev/null
cp libminiupnpc_filement.a ../../libminiupnpc.a && \
cd .. && \
rm -r "$src"
